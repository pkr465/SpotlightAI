from __future__ import annotations
from ..state import PromoState

async def run(state: PromoState) -> PromoState:
    # Real impl would add overlays, titles, mix, format/AR variants
    return state
